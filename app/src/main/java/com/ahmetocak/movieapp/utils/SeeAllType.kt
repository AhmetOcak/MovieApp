package com.ahmetocak.movieapp.utils

/**
 * Enum class representing different types for "See All" scenarios, typically used in movie listings.
 */
enum class SeeAllType {
    NOW_PLAYING,
    POPULAR
}